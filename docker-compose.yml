version: "3.8"

services:
  olake-dev:
    build:
      context: .
      dockerfile: devcontainer/Dockerfile
    container_name: olake-dev
    volumes:
      - .:/workspace
    env_file:
      - .env
    stdin_open: true
    tty: true
